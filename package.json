{
  "name": "ng5-file-drop",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "transpile": "ngc",
    "package": "rollup -c",
    "minify": "uglifyjs dist/bundles/file-drop.umd.js --screw-ie8 --compress --mangle --comments --output dist/bundles/file-drop.umd.min.js",
    "build": "npm run transpile && npm run package && npm run minify"
  },
  "private": true,
  "dependencies": {
    "@angular/common": "^5.0.3",
    "@angular/core": "^5.0.3"
  },
  "devDependencies": {
    "@angular/compiler": "^5.0.3",
    "@angular/compiler-cli": "^5.0.3",
    "rollup": "^0.52.0",
    "typescript": "^2.6.2",
    "uglify-js": "^3.2.0"
  }
}
